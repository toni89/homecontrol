<div class="boxed">
    <div class="title-bar white">
        <h4>Time Trigger</h4>
    </div>

    <div class="inner no-radius">
        <form method="post" action="#" class="basic-form">
            <div class="col-lg-6 col-md-6 col-sm-12">
                <label for="name">Time Trigger:</label>
                <hr>
                {{input type="checkbox" name=defaultConfig.specialDateBool checked=event.event.specialDateBool}}
                Special Date:
                {{input type=text name=defaultConfig.start_date id=event.event.start_date placeholder="DD.MM.YYYY" value=event.event.start_date}}
                {{input type=text name=defaultConfig.start_time id=event.event.start_time placeholder="hh:mm" value=event.event.start_time}}
                <hr>
                {{input type="checkbox" name=defaultConfig.repeatDateBool checked=event.event.repeatDateBool}}
                Repeat Date:
                {{input type=text name=defaultConfig.repeat_time id=event.event.repeat_time placeholder="hh:mm" value=event.event.repeat_time}}

                {{view Ember.Select
                content=timetriggers
                prompt="Please select"
                selectionBinding="selectedTimeRepeat"
                optionValuePath="content.value"
                optionLabelPath="content.name"
                }}

            </div>
        </form>
    </div>
</div>
